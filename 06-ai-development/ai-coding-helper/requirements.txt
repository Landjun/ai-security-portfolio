openai>=1.40.0
python-dotenv>=1.0.0
fastembed>=0.3.0       # RAG 本地 embedding(CPU 友好,无需 GPU/torch)
numpy>=1.24.0          # RAG 向量检索(余弦相似度)
fastapi>=0.110.0       # Web 服务(SSE 流式接口)
uvicorn>=0.27.0        # ASGI 服务器(运行 FastAPI)
chromadb>=0.5.0        # 真实向量数据库(持久化检索,--vectordb 时用)
