# 06 · AI 开发 (AI Development)

> "会造才会防"。本模块沉淀**真实的 AI 开发能力**——调用真实大模型、做真实 RAG、搭真实 Agent、跑通微调。
> 这是支撑 AI 内生安全研究(模块 07)的地基。

## 计划中的项目(阶段 1)

- [ ] **1.1 真实 LLM 应用**:调用真实大模型 API 或本地开源模型,结构化输出 + prompt 工程
- [ ] **1.2 真实 RAG 系统**:embedding + 向量库(FAISS/Chroma),可检索问答 + 检索质量评估
- [ ] **1.3 真实 Agent**:function calling / ReAct,多工具协作(LangChain/LlamaIndex/MCP)
- [ ] **1.4 微调入门**:LoRA 微调一个小模型,数据集 → 训练 → 评测全流程

## 与安全模块的关系

- 模块 `02/03` 的攻防之前用"模拟模型",在这里换成**真实模型**后,攻防会更有说服力。
- 模块 `07` 的内生安全实验(对抗/投毒/后门)都需要先有可训练/可调用的真实模型。

## 约定

每个项目独立子目录,含 `README.md` + 可运行代码。涉及 API Key 一律走环境变量,**绝不提交密钥**(见根目录 `.gitignore`)。

> 当前为占位,阶段 1 待开工。
